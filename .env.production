# Production Environment Template
# This file serves as a template. Actual values are set during deployment.
# The deployment script will create .env.production.local with real values.

# API URL will be auto-detected or set via deployment script
# REACT_APP_API_URL=http://your-domain.com:4005
REACT_APP_ENV=production

# Security settings
GENERATE_SOURCEMAP=false
REACT_APP_DEBUG=false
